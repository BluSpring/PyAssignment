from src.main import main

# Call the main function in src. If the src/main.py file was run directly, it may cause issues unless the working directory was manually set to the root directory,
# so it's easier to have the root directory as the working directory immediately.
main()